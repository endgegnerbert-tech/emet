# emet Codex plugin bundle

This directory is the local Codex marketplace plugin source. It bootstraps `@black-knight.dev/emet` on first run and then starts the MCP server with the Codex host profile.
