# Cursor

## Install

```bash
npm install -g @black-knight.dev/emet
emet init cursor --write
```

## Config file

Repo example: [`configs/cursor/mcp.json`](../../configs/cursor/mcp.json)

## Verify

Restart Cursor, then confirm the `emet` MCP server is enabled and exposes `emet`.
